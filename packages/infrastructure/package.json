{
  "name": "@aegis-ai/infrastructure",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc && npm run bundle",
    "bundle": "node scripts/bundle-lambdas.js",
    "clean": "rimraf dist cdk.out",
    "cdk": "cdk",
    "deploy": "cdk deploy --all --require-approval never",
    "synth": "cdk synth",
    "diff": "cdk diff",
    "test": "echo \"No tests in infrastructure\""
  },
  "dependencies": {
    "aws-cdk-lib": "^2.120.0",
    "constructs": "^10.3.0",
    "source-map-support": "^0.5.21"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "esbuild": "^0.19.0",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.3"
  }
}
