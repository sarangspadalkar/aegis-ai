/**
 * Optional: bundle Lambda packages with esbuild if not using NodejsFunction.
 * Currently we use NodejsFunction so this script is a no-op placeholder.
 */
console.log('Lambda bundling is handled by CDK NodejsFunction.');
process.exit(0);
